name: CI

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Repo hygiene
        run: |
          test -f README.md
          test -f ARCHITECTURE.md
          test -f DECISIONS.md
